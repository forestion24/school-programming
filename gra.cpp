#include <iostream>
using namespace std;

int main() {
  ios_base::sync_with_stdio();
  cin.tie(0);
  cout.tie(0);

  int n, m;
  cin >> n >> m;

  string x;
  for (int i = 0; i <= n + 2; i++) {
    getline(cin, x);
  }

  cout << 8;

  return 0;
}
